// Tools
import { styled } from "@mui/material"
// Styled components
export default styled("span")(({ theme }) => ({
    width: "100%",
    height: "128px",
    fontWeight: 500,
    fontSize: "24px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
}))
